import React from 'react'

const Button = ({ppp}) => {
  return (
    <div>
      <button className='bg-yellow-900 border-2 border-black '  > hello + {ppp ? ppp : "base"} fuc</button>
    </div>
  )
}

export default Button
